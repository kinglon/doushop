var jsResult = {};
jsResult['fun'] = 'get_login_info';
try 
{  
  jsResult['token'] = JSON.parse(sessionStorage.getItem('storeGetters'))['user']['token']
  jsResult['cookie'] = document.cookie; 
} 
catch (error) 
{
  
}
jsResult;